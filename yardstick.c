#include "main.h"
/**
 * yardstick - measures length of string
 * @s: string measured
 * Return: returns length of string
*/

int yardstick(char *s)
{
	int x = 0;

	while (s[x] != '\0')
		i++;
	return (i);
}